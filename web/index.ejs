<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, user-scalable=no">
    <title>SynthStick</title>
    <style>
      body {
        background: linear-gradient(#333, #111);
      }
  
      #wrapper {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }
  
      #frame {
        box-shadow: 0 0 32px rgba(0, 0, 0, 0.5);
      }
  
      #titlebar {
        display: flex;
        align-items: center;
        background: #fff;
        font-family: sans-serif;
        font-size: 0.8rem;
        padding: 0.25rem 0.5rem;
      }
  
      #titlebar > div {
        margin-left: auto;
        font-size: 1.2rem;
      }
  
      #app {
        width: 250px;
        height: 371px;
      }
  
      #download {
        margin: 1.5rem 0;
        text-align: center;
        font-family: sans-serif;
        color: #eee;
      }
  
      #download > h4 {
        color: #999;
      }
  
      #download > a {
        display: inline-block;
        background: #333;
        color: #eee;
        border: 1px solid #000;
        box-shadow: 0 0 8px rgba(0, 0, 0, 0.5);
        padding: 0.5rem 1rem;
        font-size: 1.4rem;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div id="wrapper">
      <div id="frame">
        <div id="titlebar">
          SynthStick
          <div>&times;</div>
        </div>
        <div id="app"></div>
      </div>
      <div id="download">
        <h2>Connect a gamepad and try it right here</h2>
        <h4>or</h4>
        <a href="https://github.com/danielesteban/synthstick/releases/latest">
          Download for Linux & Windows
        </a>  
      </div>
    </div>
  </body>
</html>
